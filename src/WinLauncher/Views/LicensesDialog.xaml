<Window x:Class="WinLauncher.Views.LicensesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="开源许可" Width="580" Height="470"
        MinWidth="480" MinHeight="340"
        WindowStartupLocation="CenterOwner"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        Background="{DynamicResource PanelBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="开源许可" FontSize="20" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"/>
        <TextBlock Grid.Row="1" Margin="0,6,0,16"
                   Text="界面图标：Microsoft Fluent System Icons（MIT）"
                   Foreground="{DynamicResource TextSecondaryBrush}"/>

        <TextBox x:Name="LicenseText" Grid.Row="2" IsReadOnly="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Background="{DynamicResource SearchBackgroundBrush}"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 BorderBrush="{DynamicResource DividerBrush}"
                 Padding="14" FontFamily="Consolas" FontSize="12"/>

        <Button Grid.Row="3" Content="关闭" IsDefault="True" IsCancel="True"
                Style="{StaticResource PrimaryButtonStyle}"
                HorizontalAlignment="Right" Margin="0,16,0,0"
                Click="OnClose"/>
    </Grid>
</Window>
